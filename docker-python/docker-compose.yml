version: '3.6'
services:
  python3:
    restart: always
    build:
      context: .
      dockerfile: ./infra/docker/python/Dockerfile
    container_name: 'python3'
    working_dir: '/home/myzn/'
    tty: true
    volumes:
      - ./src/:/home/myzn/src
    environment:
      - TZ=Asia/Tokyo
